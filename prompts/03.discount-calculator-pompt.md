# Mission 3 — Context-Rich Debugging

## Issue / Branch / PR
- Issue: `#3 — Debug incorrect discount calculation`
- Branch: `fix/discount-calculation`
- PR: Add the real PR link.

## Deliberately Broken Starting Code

```js
function calculateDiscount(price, discountPercentage) {
  const discountAmount = price * (discountPercentage / 100);

  // Intentional bug: the discount is added instead of subtracted.
  return price + discountAmount;
}

module.exports = { calculateDiscount };
```

## Reproduction Evidence
- Test input: `price = 100`, `discountPercent = 20`
- Expected result: `80`
- Actual broken result: `99.8`

## Debugging Prompt

```text
Act as an experienced JavaScript debugging engineer.

Here is a broken function.

Expected Result:
20% discount on 100 should return 80.

Actual Result:
The function returns 120.

Input:

price = 100

discount = 20

Please:

Identify the bug.

Explain why it occurs.

Apply the smallest possible fix.

Do not rewrite the whole function.

Finally provide manual test cases.
```

## Expected AI Diagnosis
The broken function subtracts `discountPercent / 100` directly from the price. It should first calculate the percentage of the price: `price * (discountPercent / 100)`.



